{
	"parameters": [
		{
			"name": "page",
			"description": "The current page requested - used for pagination",
			"in": "query",
			"required": false,
			"schema": {
				"type": "string",
				"default": "1"
			}
		},
		{
			"name": "maxrows",
			"description": "The max rows per page - used for pagination",
			"in": "query",
			"required": false,
			"schema": {
				"type": "string",
				"default": "25"
			}
		},
		{
			"name": "question",
			"description": "A filter for question",
			"in": "query",
			"required": false,
			"schema": {
				"type": "string",
				"default": ""
			}
		}
	]
}
